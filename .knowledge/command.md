## 0. Command 

```bash
// Help
npx nest --help

// Tạo Module, Controller, Service, Dto, Entity
npx nest g res users

Các lệnh tạo lẻ:
+ controller
  npx nest g co users 
+ service
  npx nest g s users 
+ middleware
  npx nest g mi users
+ guard 
  nenpx nestst g gu users 
+ interceptor
  npx nest g in users
+ filter
  npx nest g f users
```
